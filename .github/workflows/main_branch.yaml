name: Push to release branch

on:
  push:
    branches:
      - test-beta
jobs:
  release:
    uses: ava-labs/extension-avalanche/.github/workflows/release.yaml@1.12
  rebase:
    step:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: rebase main
        runs-on: ubuntu-latest
        run: |
          git checkout test-alpha
          git rebase test-beta
          git push origin head
