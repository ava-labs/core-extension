import { HorizontalFlex } from '@avalabs/react-components';
import styled from 'styled-components';

export const BalanceColumn = styled(HorizontalFlex)`
  text-align: right;
  padding-left: 8px;
  line-height: 14px;
`;
